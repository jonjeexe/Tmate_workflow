name: Cloudflare SSH Debug Session (India Optimized)

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup SSH Access (limit to actor only)
        run: |
          mkdir -p ~/.ssh
          # Only fetch SSH keys of the person who triggered the workflow
          curl -s https://github.com/${{ github.actor }}.keys >> ~/.ssh/authorized_keys
          chmod 700 ~/.ssh
          chmod 600 ~/.ssh/authorized_keys
          echo "SSH keys added for: ${{ github.actor }}"

      - name: Install Cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x ./cloudflared
          sudo mv ./cloudflared /usr/local/bin/cloudflared
          cloudflared --version

      - name: Start Cloudflare Tunnel & Print SSH Command
        run: |
          # Start tunnel in background and log output
          cloudflared tunnel --url tcp://localhost:22 > /tmp/cf.log 2>&1 &

          # Wait for tunnel to be ready
          echo "Waiting for tunnel..."
          for i in $(seq 1 15); do
            CF_URL=$(grep -o 'https://.*\.trycloudflare\.com' /tmp/cf.log | head -1)
            if [ -n "$CF_URL" ]; then
              break
            fi
            sleep 2
          done

          if [ -z "$CF_URL" ]; then
            echo "Tunnel failed to start. Log:"
            cat /tmp/cf.log
            exit 1
          fi

          echo "============================================"
          echo "Tunnel is ready!"
          echo ""
          echo "STEP 1 - Install cloudflared on Termux:"
          echo "  pkg install cloudflared"
          echo ""
          echo "STEP 2 - Connect via SSH:"
          echo "  ssh -o ProxyCommand='cloudflared access tcp --hostname ${CF_URL}' runner@localhost"
          echo ""
          echo "Tunnel URL: ${CF_URL}"
          echo "Triggered by: ${{ github.actor }}"
          echo "============================================"

          # Keep session alive for 6 hours
          sleep 21600
